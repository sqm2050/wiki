分组交换:
	分组交换（packet switching）在计算机网络和通讯中是一种相对于电路交换的
	通讯范例，分组（又称消息、或消息碎片）在节点间单独路由，不需要在传输前
	先建立通信路径。

报文交换：
	报文交换（message  switching），又称存储转发交换，是数据交换的三种方式
	之一，报文整个的发送，一次一跳。
	主要特点：存储接受到的报文，判断目标地址以选择路由，最后，在下一跳路由
	空闲时，将数据转发给下一跳路由。
