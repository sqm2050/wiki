NOR flash之所以可以片内执行，就是因为他符合CPU取指令译码的要求。CPU送一个地址出
来，NOR flash就能给一个数据让CPU执行，中间不需要额外的处理操作。

NAND flash不一样因为nand flash有地址，数据，命令共用IO口的问题，cpu把地址发出来
之后，并不能直接得到数据，还需要控制线的操作才能完成。就是他没有专用SRAM接口。

芯片内执行主要是看芯片可不可以线性存储代码(假如硬件支持芯片支持).只要能保证芯片
的存储空间是线性的(也就是无坏块),都可以片上执行。
