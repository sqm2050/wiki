boyer moore字符串匹配算法

#坏字符规则
* "坏字符" 对应在搜索词的第m位，在搜索词中的上一次出现位未n，所以后移位m - n位。
* 如果没有在搜索词中出现那么它的位置是-1，则整个搜索词后移。

#好后缀规则
* 好后缀的位置以好后缀的最后一个字符为准。
* 如果好后缀在搜索词中没有重复出现，则它的上一次出现位置为-1。



	``` 
	adadadadda
  	adadadadad
	dada 
	```
